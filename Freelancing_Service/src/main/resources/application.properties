# Application Properties
spring.application.name=Freelance Marketplace

# Database Configuration (Supabase PostgreSQL)
spring.datasource.url=jdbc:postgresql://db.ilihhlyksmmteyseotbq.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=?K6V/73&rYFFrT+
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Disable Spring Boot's auto-dialect detection
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database=postgresql

# Mail Configuration (TEMPORARILY DISABLE)
spring.mail.host=localhost
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# JWT Configuration
app.jwt.secret=1234567890123456789012345678901234567890123456789012345678901234
app.jwt.expiration-ms=86400000

# Platform Configuration
platform.fee.percentage=10
platform.currency=USD

# Stripe Configuration
stripe.secret-key=sk_test_dummy_key_for_testing_only
stripe.public-key=pk_test_dummy_key_for_testing_only

# CORS Configuration
app.cors.allowed-origins=https://freelancer-job-market-place-spring.vercel.app

# File Upload Directory
app.file.upload-dir=./uploads

# Logging
logging.level.com.marketplace=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=ERROR

# Allow bean overriding
spring.main.allow-bean-definition-overriding=true

# PostgreSQL connection settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5