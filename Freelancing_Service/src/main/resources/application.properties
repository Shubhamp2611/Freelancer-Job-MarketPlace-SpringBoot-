# Application Properties
spring.application.name=Freelance Marketplace

# Database Configuration (Supabase PostgreSQL)
spring.datasource.url=jdbc:postgresql://db.ilihhlyksmmteyseotbq.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=?K6V/73&rYFFrT+   # replace with your Supabase password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Mail Configuration (comment out if not using email)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shree22kar2411@gmail.com
spring.mail.password=zsxdcfvgb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server Configuration
server.port=8080

# JWT Configuration
app.jwt.secret=1234567890123456789012345678901234567890123456789012345678901234
app.jwt.expiration-ms=86400000

# Platform Configuration (used by PaymentService)
platform.fee.percentage=10
platform.currency=USD

# Stripe Configuration (used by PaymentService)
stripe.secret-key=sk_test_dummy_key_for_testing_only
stripe.public-key=pk_test_dummy_key_for_testing_only

# CORS Configuration
app.cors.allowed-origins=https://freelancer-job-market-place-spring.vercel.app


# File Upload Directory
app.file.upload-dir=./uploads

# Logging
logging.level.com.marketplace=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Allow bean overriding (temporary)
spring.main.allow-bean-definition-overriding=true