services:
  - type: web
    name: freelance-marketplace
    env: java  # ← This tells Render to use Java buildpack
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/freelance-marketplace-api.jar
    envVars:
      - key: JAVA_VERSION
        value: 17